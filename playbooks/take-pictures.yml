---
- hosts: gehan-bramble
  gather_facts: False
  remote_user: pi
  ignore_errors: True

  tasks:
    - name: Take picture on the raspberry pi.
      shell: python /home/pi/camera_single.py && echo Success
      register: command_result
      changed_when: "'Success' in command_result.stdout"

    - mail:
        host: smtp.gmail.com
        port: 587
        username: gehan.bramble@gmail.com
        password: #password
        to: gehan.bramble@gmail.com
        subject: "Image Failed Capture"
        body: "Error in Copy to Master"
        when: "'Success' in command_result.stderr"

